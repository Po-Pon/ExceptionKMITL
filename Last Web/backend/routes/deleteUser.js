const express = require("express");
const pool = require("../config");

router = express.Router();

router.post('http://localhost:5000/delete/:id', async function (req, res, next){
    
})




exports.router = router;